---
description: Beispiel-Slash-Command – kann angepasst oder gelöscht werden
---

Dies ist ein Beispiel für einen benutzerdefinierten Slash-Command.

Aufruf in Claude Code mit `/example`.

Argumente stehen über `$ARGUMENTS` zur Verfügung: $ARGUMENTS

Beschreibe hier die Aufgabe, die Claude beim Aufruf dieses Commands ausführen soll.
